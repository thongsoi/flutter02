<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.INTERNET" /> <!-- If your app needs to access the internet, add this permission. -->
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!-- If your app needs to check network connectivity, add this permission. -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/> <!-- If your app needs to display content on top of other apps, add this permission. -->
<uses-permission android:name="android.permission.VIBRATE"/> <!-- If your app needs to control the device vibrator, add this permission. -->
<uses-permission android:name="android.permission.WAKE_LOCK"/> <!-- If your app needs to keep the device awake, add this permission. -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE"/> <!-- If your app uses foreground services, add this permission. -->
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/> <!-- If your app needs to access Wi-Fi state, add this permission. -->
<uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/> <!-- If your app needs to change Wi-Fi state, add this permission. -->
<uses-permission android:name="android.permission.BLUETOOTH"/> <!-- If your app needs to use Bluetooth, add this permission. -->
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/> <!-- If your app needs to manage Bluetooth, add this permission. -->
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"/> <!-- If your app needs to scan for Bluetooth devices, add this permission. -->
<uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/> <!-- If your app needs to advertise over Bluetooth, add this permission. -->
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/> <!-- If your app needs to connect to Bluetooth devices, add this permission. -->
<uses-permission android:name="android.permission.CAMERA"/> <!-- If your app needs to use the camera, add this permission. -->
<uses-permission android:name="android.permission.RECORD_AUDIO"/> <!-- If your app needs to record audio, add this permission. -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/> <!-- If your app needs to read from external storage, add this permission. -->
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> <!-- If your app needs to write to external storage, add this permission. -->
<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/> <!-- If your app needs to manage all files on external storage, add this permission. -->   
    <application
        android:label="frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
